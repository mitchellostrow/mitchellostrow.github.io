# mitchellostrow.github.io

The whole site is `index.html`. To preview locally, run `python3 -m http.server 8000` and open http://localhost:8000.
Pushing to `master` deploys it to the `gh-pages` branch (`.github/workflows/deploy.yml`).
